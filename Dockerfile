# 1. Используем официальный базовый образ Node.js (версия 18)
FROM node:18

# 2. Устанавливаем рабочую директорию в контейнере
WORKDIR /app

# 3. Копируем файлы проекта
COPY package*.json ./
COPY server.js ./

# 4. Устанавливаем зависимости Node.js
RUN npm install

# 5. Определяем команду для запуска приложения
CMD [ "node", "server.js" ]